/**
 * Icons are vendored as prebuilt bundles under ./bundled.
 * No compile step is required; this script exists so workspace
 * `build` hooks remain consistent with other packages.
 */
console.log('@blockexpanse/icons: prebuilt, skip compile');
