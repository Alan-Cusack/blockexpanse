{
  "name": "blockexpanse-sync-server",
  "version": "0.0.1",
  "private": true,
  "description": "Reference WebSocket sync server for BlockExpanse collaboration (Cloudflare Worker + Durable Objects)",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  }
}
